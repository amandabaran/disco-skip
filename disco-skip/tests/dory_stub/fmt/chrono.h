#pragma once
// Compile-only stub. latency.hpp includes this for duration formatting; the
// core stub's variadic print/format already accept anything, so there is
// nothing further to declare.
#include <fmt/core.h>
